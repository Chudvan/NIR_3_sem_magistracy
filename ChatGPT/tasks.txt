1) На основе данных, описанных в п. 1) - 6) файла 'dataset_description.txt' - построить регрессионные модели, которые будут учитывать формульные зависимости из п. 5). Входные данные - из столбцов facs_fields (42 столбца), выходные - из столбцов seven_fields (7 столбцов) // преобразование 42 -> 7
2) Обратные регрессионные модели: Входные данные (7 столбцов) - из столбцов seven_fields, выходные - из столбцов facs_fields (42 столбца) // преобразование 7 -> 42
3) Проверить гипотезу из п. 6) файла 'dataset_description.txt', с помощью PCA. При необходимости, добавить к PCA преобразованию регрессионную зависимость, чтобы провести обучение с учителем: Входные данные (7 столбцов) - из столбцов seven_fields, выходные - из столбцов pa_fields (2 столбца) // преобразование 7 -> 2
